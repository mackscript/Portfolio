import React, { useEffect } from 'react';
import { TimelineLite, gsap, CSSPlugin, Power3, Expo } from 'gsap';
import SplitType from 'split-type';

const Header = () => {
  const rows = 1;
  const columns = 10;

  return <div className='grid_container darkMode'></div>;
};

export default Header;
